# CCTV CAD 覆盖图生成 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 创建 CLI 工具，输入摄像机参数，输出 DXF 格式平面覆盖图（DORI 分层 + 盲区 + 刻度）

**Architecture:** ezdxf 生成 DXF，constants.py 存储传感器/DORI 常量，draw_coverage.py 处理参数解析、计算、绘图。作为 cli-anything 子技能集成。

**Tech Stack:** Python 3, ezdxf

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `cli-anything/sub-skills/cctv-cad/scripts/constants.py` | 传感器尺寸表、DORI 标准、默认俯角映射 |
| `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py` | 主脚本：argparse → 计算 → DXF 生成 |
| `cli-anything/sub-skills/cctv-cad/SKILL.md` | 技能描述 + 触发词 |
| `cli-anything/SKILL.md` | 路由表新增 cctv-cad 行 |

---

### Task 1: 创建常量模块

**Files:**
- Create: `cli-anything/sub-skills/cctv-cad/scripts/constants.py`

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p cli-anything/sub-skills/cctv-cad/scripts
```

- [ ] **Step 2: 创建 constants.py**

```python
"""传感器尺寸、DORI 标准、默认俯角常量"""

# 传感器物理尺寸 (宽mm × 高mm)
# 来源: 安防监控知识项目 + IEC 62676-4
SENSORS = {
    "1/3":   {"w": 4.8,   "h": 3.6},
    "1/2.8": {"w": 5.12,  "h": 3.84},
    "1/2.7": {"w": 5.3,   "h": 4.0},
    "1/2.5": {"w": 5.76,  "h": 4.32},
    "1/2":   {"w": 6.4,   "h": 4.8},
    "1/1.8": {"w": 7.18,  "h": 5.4},
    "1/1.7": {"w": 7.6,   "h": 5.7},
    "2/3":   {"w": 8.8,   "h": 6.6},
}

# DORI 像素密度标准 (px/m)
# 来源: IEC 62676-4
DORI = {
    "I": {"name": "辨识", "px_per_m": 250, "color": 1},   # 红色
    "R": {"name": "识别", "px_per_m": 125, "color": 30},  # 橙色
    "O": {"name": "观察", "px_per_m": 62,  "color": 2},   # 黄色
    "D": {"name": "探测", "px_per_m": 25,  "color": 3},   # 绿色
}

# 像素映射 (水平像素数)
PIXELS = {
    "2mp": 1920,
    "4mp": 2560,
    "8mp": 3840,
}

# 默认俯角 (按焦距)
DEFAULT_TILT = {
    2.8: 25,
    4:   30,
    6:   35,
    8:   40,
    12:  50,
}

# DXF 图层颜色
LAYER_COLORS = {
    "CAMERA":     7,   # 白色
    "FOV":        4,   # 青色
    "DORI-I":     1,   # 红色
    "DORI-R":     30,  # 橙色
    "DORI-O":     2,   # 黄色
    "DORI-D":     3,   # 绿色
    "BLINDSPOT":  1,   # 红色
    "ANNOTATION": 8,   # 灰色
}

# 刻度间距 (米)
SCALE_INTERVAL = 5
```

- [ ] **Step 3: 验证导入**

```bash
cd cli-anything/sub-skills/cctv-cad/scripts
py -3 -c "from constants import SENSORS, DORI, PIXELS; print('OK')"
```

Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/
git commit -m "feat(cctv-cad): add constants module with sensor/DORI data"
```

---

### Task 2: 创建主脚本 — 参数解析

**Files:**
- Create: `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py`

- [ ] **Step 1: 创建 draw_coverage.py 骨架（argparse）**

```python
#!/usr/bin/env python3
"""CCTV 摄像机覆盖范围平面图生成工具"""

import argparse
import math
import sys
from pathlib import Path

import ezdxf

from constants import SENSORS, DORI, PIXELS, DEFAULT_TILT, LAYER_COLORS, SCALE_INTERVAL


def parse_args():
    parser = argparse.ArgumentParser(
        description="生成摄像机覆盖范围 DXF 平面图",
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument("--focal", type=float, default=4,
                        help="焦距 (mm): 2.8/4/6/8/12, 默认 4")
    parser.add_argument("--pixels", default="4mp", choices=["2mp", "4mp", "8mp"],
                        help="像素: 2mp/4mp/8mp, 默认 4mp")
    parser.add_argument("--sensor", default="1/2.8",
                        help="传感器尺寸, 默认 1/2.8")
    parser.add_argument("--height", type=float, default=3.0,
                        help="安装高度 (m), 默认 3.0")
    parser.add_argument("--tilt", default="auto",
                        help="俯角 (°), auto 按焦距推荐, 默认 auto")
    parser.add_argument("--direction", type=float, default=0,
                        help="摄像机朝向角度 (°), 0=右 90=上 180=左 270=下, 默认 0")
    parser.add_argument("--output", "-o", required=True,
                        help="输出 DXF 路径")
    parser.add_argument("--dry-run", action="store_true",
                        help="显示计算参数但不生成文件")
    parser.add_argument("--no-dori", action="store_true",
                        help="去掉 DORI 分层")
    parser.add_argument("--no-blindspot", action="store_true",
                        help="去掉盲区标注")
    return parser.parse_args()


def validate_args(args):
    """验证参数，返回修正后的参数"""
    errors = []

    if args.focal not in [2.8, 4, 6, 8, 12]:
        errors.append(f"焦距不支持: {args.focal}, 支持: 2.8/4/6/8/12")

    if args.sensor not in SENSORS:
        errors.append(f"传感器不支持: {args.sensor}, 支持: {list(SENSORS.keys())}")

    if args.height <= 0:
        errors.append(f"安装高度必须为正数: {args.height}")

    if errors:
        print("参数错误:", file=sys.stderr)
        for e in errors:
            print(f"  - {e}", file=sys.stderr)
        sys.exit(1)

    # 处理 auto 俯角
    if args.tilt == "auto":
        args.tilt = DEFAULT_TILT.get(args.focal, 30)
    else:
        args.tilt = float(args.tilt)

    return args


def calculate(args):
    """计算 FOV、DORI 距离、盲区"""
    sensor = SENSORS[args.sensor]
    focal = args.focal
    h_pixels = PIXELS[args.pixels]
    height = args.height
    tilt = args.tilt

    # 水平/垂直视场角 (度)
    h_fov = math.degrees(2 * math.atan(sensor["w"] / (2 * focal)))
    v_fov = math.degrees(2 * math.atan(sensor["h"] / (2 * focal)))

    # DORI 距离
    dori_distances = {}
    for level, info in DORI.items():
        d = (h_pixels * focal) / (sensor["w"] * info["px_per_m"])
        dori_distances[level] = d

    # 盲区深度 (米)
    blind_depth = height / math.tan(math.radians(tilt + v_fov / 2))

    return {
        "h_fov": h_fov,
        "v_fov": v_fov,
        "dori": dori_distances,
        "blind_depth": blind_depth,
    }


def print_dry_run(args, calc):
    """打印计算参数"""
    print(f"焦距:     {args.focal} mm")
    print(f"像素:     {args.pixels} ({PIXELS[args.pixels]} px)")
    print(f"传感器:   {args.sensor} ({SENSORS[args.sensor]['w']}mm)")
    print(f"安装高度: {args.height} m")
    print(f"俯角:     {args.tilt}°")
    print(f"朝向:     {args.direction}°")
    print()
    print(f"水平 FOV: {calc['h_fov']:.1f}°")
    print(f"垂直 FOV: {calc['v_fov']:.1f}°")
    print(f"盲区深度: {calc['blind_depth']:.2f} m")
    print()
    print("DORI 距离:")
    for level, dist in calc["dori"].items():
        info = DORI[level]
        print(f"  {level} ({info['name']}): {dist:.1f} m")


def create_dxf(args, calc):
    """创建 DXF 文档并绘制覆盖图"""
    doc = ezdxf.new("R2010")
    doc.units = ezdxf.units.M
    msp = doc.modelspace()

    # 创建图层
    for name, color in LAYER_COLORS.items():
        doc.layers.add(name, color=color)

    # TODO: 绘制实体
    # 会在后续步骤实现

    return doc


def main():
    args = parse_args()
    args = validate_args(args)
    calc = calculate(args)

    if args.dry_run:
        print_dry_run(args, calc)
        return

    doc = create_dxf(args, calc)
    doc.saveas(args.output)
    print(f"已生成: {args.output}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 验证参数解析**

```bash
cd cli-anything/sub-skills/cctv-cad/scripts
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --tilt auto --direction 0 --output test.dxf --dry-run
```

Expected:
```
焦距:     4 mm
像素:     4mp (2560 px)
传感器:   1/2.8 (5.12mm)
安装高度: 3.0 m
俯角:     30°
朝向:     0°

水平 FOV: 65.2°
垂直 FOV: 51.0°
盲区深度: 1.15 m

DORI 距离:
  I (辨识): 12.5 m
  R (识别): 25.0 m
  O (观察): 50.4 m
  D (探测): 124.9 m
```

- [ ] **Step 3: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py
git commit -m "feat(cctv-cad): add main script with arg parsing and calculations"
```

---

### Task 3: 实现 DXF 绘制 — 扇形和 DORI 分层

**Files:**
- Modify: `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py`

- [ ] **Step 1: 添加扇形绘制函数**

在 `draw_coverage.py` 中添加：

```python
def polar_to_cartesian(cx, cy, radius, angle_deg):
    """极坐标转笛卡尔坐标"""
    angle_rad = math.radians(angle_deg)
    x = cx + radius * math.cos(angle_rad)
    y = cy + radius * math.sin(angle_rad)
    return x, y


def draw_arc(msp, layer, cx, cy, radius, start_angle, end_angle, segments=64):
    """绘制圆弧（用 LWPOLYLINE 近似）"""
    points = []
    for i in range(segments + 1):
        angle = math.radians(start_angle + (end_angle - start_angle) * i / segments)
        x = cx + radius * math.cos(angle)
        y = cy + radius * math.sin(angle)
        points.append((x, y))
    msp.add_lwpolyline(points, dxfattribs={"layer": layer})


def draw_sector(msp, layer, cx, cy, radius, start_angle, end_angle, segments=64):
    """绘制扇形区域（用于 DORI 分层）"""
    points = [(cx, cy)]  # 起点：圆心
    for i in range(segments + 1):
        angle = math.radians(start_angle + (end_angle - start_angle) * i / segments)
        x = cx + radius * math.cos(angle)
        y = cy + radius * math.sin(angle)
        points.append((x, y))
    points.append((cx, cy))  # 回到圆心
    msp.add_lwpolyline(points, dxfattribs={"layer": layer})
```

- [ ] **Step 2: 实现 draw_dori_layers 函数**

```python
def draw_dori_layers(msp, args, calc):
    """绘制 DORI 分层扇形"""
    cx, cy = 0, 0  # 摄像机位置
    direction = args.direction
    h_fov = calc["h_fov"]
    start_angle = direction - h_fov / 2
    end_angle = direction + h_fov / 2

    # 从远到近绘制（D → O → R → I）
    layers_order = ["D", "O", "R", "I"]
    prev_radius = 0

    for level in layers_order:
        radius = calc["dori"][level]
        layer_name = f"DORI-{level}"

        # 绘制当前层的扇形环
        # 简化：绘制扇形（从圆心到当前半径）
        draw_sector(msp, layer_name, cx, cy, radius, start_angle, end_angle)

        prev_radius = radius
```

- [ ] **Step 3: 更新 create_dxf 调用绘制**

修改 `create_dxf` 函数：

```python
def create_dxf(args, calc):
    """创建 DXF 文档并绘制覆盖图"""
    doc = ezdxf.new("R2010")
    doc.units = ezdxf.units.M
    msp = doc.modelspace()

    # 创建图层
    for name, color in LAYER_COLORS.items():
        doc.layers.add(name, color=color)

    # 绘制 DORI 分层
    if not args.no_dori:
        draw_dori_layers(msp, args, calc)

    return doc
```

- [ ] **Step 4: 测试生成 DXF**

```bash
cd cli-anything/sub-skills/cctv-cad/scripts
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output test_coverage.dxf
```

Expected: 生成 test_coverage.dxf 文件

- [ ] **Step 5: 验证 DXF 内容**

```bash
py -3 -c "
import ezdxf
doc = ezdxf.readfile('test_coverage.dxf')
msp = doc.modelspace()
entities = list(msp)
print(f'实体数量: {len(entities)}')
layers = [e.dxf.layer for e in entities]
print(f'图层: {set(layers)}')
"
```

Expected: 实体数量 > 0，图层包含 DORI-*

- [ ] **Step 6: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py
git commit -m "feat(cctv-cad): add DORI sector drawing"
```

---

### Task 4: 实现视场角边界线

**Files:**
- Modify: `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py`

- [ ] **Step 1: 添加 FOV 边界线绘制**

```python
def draw_fov_lines(msp, args, calc):
    """绘制视场角边界线"""
    cx, cy = 0, 0
    direction = args.direction
    h_fov = calc["h_fov"]
    max_radius = calc["dori"]["D"]  # 用最大 DORI 距离

    # 两条边界线
    start_angle = direction - h_fov / 2
    end_angle = direction + h_fov / 2

    x1, y1 = polar_to_cartesian(cx, cy, max_radius, start_angle)
    x2, y2 = polar_to_cartesian(cx, cy, max_radius, end_angle)

    msp.add_line((cx, cy), (x1, y1), dxfattribs={"layer": "FOV"})
    msp.add_line((cx, cy), (x2, y2), dxfattribs={"layer": "FOV"})
```

- [ ] **Step 2: 在 create_dxf 中调用**

```python
def create_dxf(args, calc):
    """创建 DXF 文档并绘制覆盖图"""
    doc = ezdxf.new("R2010")
    doc.units = ezdxf.units.M
    msp = doc.modelspace()

    # 创建图层
    for name, color in LAYER_COLORS.items():
        doc.layers.add(name, color=color)

    # 绘制 DORI 分层
    if not args.no_dori:
        draw_dori_layers(msp, args, calc)

    # 绘制 FOV 边界线
    draw_fov_lines(msp, args, calc)

    return doc
```

- [ ] **Step 3: 测试**

```bash
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output test_fov.dxf
```

- [ ] **Step 4: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py
git commit -m "feat(cctv-cad): add FOV boundary lines"
```

---

### Task 5: 实现盲区绘制

**Files:**
- Modify: `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py`

- [ ] **Step 1: 添加盲区绘制函数**

```python
def draw_blindspot(msp, args, calc):
    """绘制盲区三角形"""
    if args.no_blindspot:
        return

    cx, cy = 0, 0
    direction = args.direction
    blind_depth = calc["blind_depth"]
    h_fov = calc["h_fov"]

    # 盲区在摄像机后方（与朝向相反）
    back_angle = direction + 180

    # 盲区三角顶点
    # 1. 摄像机位置
    # 2. 后方左侧点
    # 3. 后方右侧点
    half_width = 0.3  # 盲区宽度 (米)

    p1 = (cx, cy)
    p2 = polar_to_cartesian(cx, cy, blind_depth, back_angle - 15)
    p3 = polar_to_cartesian(cx, cy, blind_depth, back_angle + 15)

    # 绘制三角形
    msp.add_lwpolyline([p1, p2, p3, p1], dxfattribs={"layer": "BLINDSPOT"})

    # 添加填充（可选，简化版先不加）
```

- [ ] **Step 2: 在 create_dxf 中调用**

```python
def create_dxf(args, calc):
    """创建 DXF 文档并绘制覆盖图"""
    doc = ezdxf.new("R2010")
    doc.units = ezdxf.units.M
    msp = doc.modelspace()

    # 创建图层
    for name, color in LAYER_COLORS.items():
        doc.layers.add(name, color=color)

    # 绘制 DORI 分层
    if not args.no_dori:
        draw_dori_layers(msp, args, calc)

    # 绘制 FOV 边界线
    draw_fov_lines(msp, args, calc)

    # 绘制盲区
    draw_blindspot(msp, args, calc)

    return doc
```

- [ ] **Step 3: 测试**

```bash
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output test_blind.dxf
```

- [ ] **Step 4: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py
git commit -m "feat(cctv-cad): add blindspot triangle"
```

---

### Task 6: 实现距离刻度

**Files:**
- Modify: `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py`

- [ ] **Step 1: 添加刻度绘制函数**

```python
def draw_scale_marks(msp, args, calc):
    """绘制距离刻度"""
    cx, cy = 0, 0
    direction = args.direction
    max_dist = calc["dori"]["D"]

    # 沿中心线绘制刻度
    for dist in range(SCALE_INTERVAL, int(max_dist) + 1, SCALE_INTERVAL):
        x, y = polar_to_cartesian(cx, cy, dist, direction)

        # 短刻度线
        perp_angle = direction + 90
        tick_len = 0.15
        x1, y1 = polar_to_cartesian(x, y, tick_len, perp_angle)
        x2, y2 = polar_to_cartesian(x, y, tick_len, perp_angle + 180)

        msp.add_line((x1, y1), (x2, y2), dxfattribs={"layer": "ANNOTATION"})

        # 距离文字
        text_x, text_y = polar_to_cartesian(x, y, 0.3, perp_angle)
        msp.add_text(
            f"{dist}m",
            dxfattribs={"layer": "ANNOTATION", "height": 0.25}
        ).set_placement((text_x, text_y))
```

- [ ] **Step 2: 在 create_dxf 中调用**

```python
def create_dxf(args, calc):
    """创建 DXF 文档并绘制覆盖图"""
    doc = ezdxf.new("R2010")
    doc.units = ezdxf.units.M
    msp = doc.modelspace()

    # 创建图层
    for name, color in LAYER_COLORS.items():
        doc.layers.add(name, color=color)

    # 绘制 DORI 分层
    if not args.no_dori:
        draw_dori_layers(msp, args, calc)

    # 绘制 FOV 边界线
    draw_fov_lines(msp, args, calc)

    # 绘制盲区
    draw_blindspot(msp, args, calc)

    # 绘制距离刻度
    draw_scale_marks(msp, args, calc)

    return doc
```

- [ ] **Step 3: 测试**

```bash
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output test_scale.dxf
```

- [ ] **Step 4: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py
git commit -m "feat(cctv-cad): add distance scale marks"
```

---

### Task 7: 实现参数标注和摄像机图标

**Files:**
- Modify: `cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py`

- [ ] **Step 1: 添加摄像机图标绘制**

```python
def draw_camera_icon(msp, args):
    """绘制摄像机位置点和方向箭头"""
    cx, cy = 0, 0
    direction = args.direction

    # 摄像机位置点
    msp.add_circle((cx, cy), radius=0.15, dxfattribs={"layer": "CAMERA"})

    # 方向箭头（短线）
    arrow_len = 0.4
    x, y = polar_to_cartesian(cx, cy, arrow_len, direction)
    msp.add_line((cx, cy), (x, y), dxfattribs={"layer": "CAMERA"})
```

- [ ] **Step 2: 添加参数标注绘制**

```python
def draw_annotation(msp, args, calc):
    """绘制参数标注"""
    # 右下角放置参数表
    base_x, base_y = 5, -2

    lines = [
        f"焦距: {args.focal} mm",
        f"像素: {args.pixels}",
        f"传感器: {args.sensor}",
        f"高度: {args.height} m",
        f"俯角: {args.tilt}°",
        f"朝向: {args.direction}°",
        f"FOV: {calc['h_fov']:.1f}°",
    ]

    for i, line in enumerate(lines):
        msp.add_text(
            line,
            dxfattribs={"layer": "ANNOTATION", "height": 0.25}
        ).set_placement((base_x, base_y - i * 0.35))
```

- [ ] **Step 3: 在 create_dxf 中调用**

```python
def create_dxf(args, calc):
    """创建 DXF 文档并绘制覆盖图"""
    doc = ezdxf.new("R2010")
    doc.units = ezdxf.units.M
    msp = doc.modelspace()

    # 创建图层
    for name, color in LAYER_COLORS.items():
        doc.layers.add(name, color=color)

    # 绘制 DORI 分层
    if not args.no_dori:
        draw_dori_layers(msp, args, calc)

    # 绘制 FOV 边界线
    draw_fov_lines(msp, args, calc)

    # 绘制盲区
    draw_blindspot(msp, args, calc)

    # 绘制距离刻度
    draw_scale_marks(msp, args, calc)

    # 绘制摄像机图标
    draw_camera_icon(msp, args)

    # 绘制参数标注
    draw_annotation(msp, args, calc)

    return doc
```

- [ ] **Step 4: 测试完整流程**

```bash
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output test_full.dxf
```

- [ ] **Step 5: 验证 DXF 实体**

```bash
py -3 -c "
import ezdxf
doc = ezdxf.readfile('test_full.dxf')
msp = doc.modelspace()
entities = list(msp)
print(f'总实体: {len(entities)}')
layers = {}
for e in entities:
    layer = e.dxf.layer
    layers[layer] = layers.get(layer, 0) + 1
for k, v in sorted(layers.items()):
    print(f'  {k}: {v}')
"
```

Expected: 各图层都有实体

- [ ] **Step 6: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/scripts/draw_coverage.py
git commit -m "feat(cctv-cad): add camera icon and parameter annotation"
```

---

### Task 8: 创建 SKILL.md

**Files:**
- Create: `cli-anything/sub-skills/cctv-cad/SKILL.md`

- [ ] **Step 1: 创建 SKILL.md**

```markdown
---
name: cctv-cad
description: 摄像机覆盖范围 CAD 平面图生成工具。输入焦距/像素/传感器/安装高度等参数，自动输出 DXF 格式平面覆盖图，包含 DORI 分层扇形、盲区标注、距离刻度。触发词：画覆盖图、CAD摄像机覆盖、cctv coverage、摄像机平面图、点位覆盖、生成覆盖范围图。
type: cli-sub
---

# CCTV CAD 覆盖图生成

摄像机覆盖范围平面图自动生成工具 — 输入参数，输出 DXF。

## Prerequisites

- **ezdxf** — `pip install ezdxf`

## Commands

### `draw coverage` — 生成覆盖范围图

```bash
python scripts/draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output camera_A1.dxf
```

**Parameters:**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--focal` | 4 | 焦距 (mm)：2.8/4/6/8/12 |
| `--pixels` | 4mp | 像素：2mp/4mp/8mp |
| `--sensor` | 1/2.8 | 传感器尺寸：1/3 ~ 2/3 |
| `--height` | 3.0 | 安装高度 (m) |
| `--tilt` | auto | 俯角 (°)，auto 按焦距推荐 |
| `--direction` | 0 | 朝向角度：0=右 90=上 180=左 270=下 |
| `--output` | - | 输出 DXF 路径 |
| `--dry-run` | false | 显示计算参数 |
| `--no-dori` | false | 去掉 DORI 分层 |
| `--no-blindspot` | false | 去掉盲区 |

**Examples:**

```bash
# 基本用法
python scripts/draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output camera_A1.dxf

# 预览参数
python scripts/draw_coverage.py --focal 6 --pixels 8mp --sensor 1/1.8 --height 5.0 --dry-run

# 简洁模式
python scripts/draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --no-dori --no-blindspot --output simple.dxf
```

## Output

DXF 文件包含以下图层：

| 图层 | 颜色 | 内容 |
|------|------|------|
| CAMERA | 白色 | 摄像机位置点 + 方向箭头 |
| FOV | 青色 | 视场角边界线 |
| DORI-I | 红色 | 辨识区域 (250 px/m) |
| DORI-R | 橙色 | 识别区域 (125 px/m) |
| DORI-O | 黄色 | 观察区域 (62 px/m) |
| DORI-D | 绿色 | 探测区域 (25 px/m) |
| BLINDSPOT | 红色 | 盲区三角形 |
| ANNOTATION | 灰色 | 距离刻度 + 参数标注 |

## DORI 标准

| 等级 | px/m | 用途 |
|------|------|------|
| I 辨识 | 250 | 人脸入库比对 |
| R 识别 | 125 | 人脸/车牌识别 |
| O 观察 | 62 | 行为分析 |
| D 探测 | 25 | 入侵检测 |

## For AI Agents

使用 `--dry-run` 验证参数再生成文件。传感器尺寸和 DORI 标准复用 cctv-focal-distance-tool 数据。
```

- [ ] **Step 2: Commit**

```bash
git add cli-anything/sub-skills/cctv-cad/SKILL.md
git commit -m "feat(cctv-cad): add SKILL.md"
```

---

### Task 9: 更新 cli-anything 路由表

**Files:**
- Modify: `cli-anything/SKILL.md`

- [ ] **Step 1: 读取现有路由表**

读取 `cli-anything/SKILL.md`，找到路由表位置。

- [ ] **Step 2: 添加 cctv-cad 行**

在路由表中添加：

```markdown
| cctv-cad | 摄像机覆盖范围 CAD 平面图生成 | 画覆盖图、CAD摄像机覆盖、cctv coverage、摄像机平面图、点位覆盖 | ... |
```

- [ ] **Step 3: Commit**

```bash
git add cli-anything/SKILL.md
git commit -m "feat(cctv-cad): add router entry to cli-anything"
```

---

### Task 10: 清理和最终测试

**Files:**
- None (测试和清理)

- [ ] **Step 1: 删除测试文件**

```bash
rm -f cli-anything/sub-skills/cctv-cad/scripts/test*.dxf
```

- [ ] **Step 2: 完整功能测试**

```bash
cd cli-anything/sub-skills/cctv-cad/scripts

# 测试1: 基本用法
py -3 draw_coverage.py --focal 4 --pixels 4mp --sensor 1/2.8 --height 3.0 --direction 0 --output final_test1.dxf

# 测试2: 不同焦距
py -3 draw_coverage.py --focal 8 --pixels 8mp --sensor 1/1.8 --height 5.0 --direction 90 --output final_test2.dxf

# 测试3: 简洁模式
py -3 draw_coverage.py --focal 6 --pixels 2mp --sensor 1/3 --height 2.5 --direction 180 --no-dori --no-blindspot --output final_test3.dxf
```

- [ ] **Step 3: 用 AutoCAD/浩辰打开验证**

手动打开 final_test1.dxf，检查：
- 扇形覆盖范围正确
- DORI 颜色分层清晰
- 盲区三角显示
- 距离刻度可读
- 参数标注完整

- [ ] **Step 4: 清理测试文件**

```bash
rm -f cli-anything/sub-skills/cctv-cad/scripts/final_test*.dxf
```

- [ ] **Step 5: Final Commit**

```bash
git add -A
git commit -m "feat(cctv-cad): complete implementation with tests"
```
